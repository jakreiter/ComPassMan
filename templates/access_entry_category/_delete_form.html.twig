<form method="post" action="{{ path('access_entry_category_delete', {'id': access_entry_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ access_entry_category.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
