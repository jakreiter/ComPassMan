<form method="post" action="{{ path('access_entry_delete', {'id': access_entry.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">

    <button class="btn btn-danger btn-block">Delete</button>
</form>
